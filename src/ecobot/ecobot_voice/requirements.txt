# Not available via rosdep/apt — install with:
#   pip3 install --user -r requirements.txt
# (matches how smbus2/aiortc/ultralytics are already installed on this workspace)
python-dotenv

# Local STT (default — STT_MODEL selects the engine, see local_engines.py)
faster-whisper               # used when STT_MODEL is a plain size like "small"
transformers                 # used when STT_MODEL is a HF repo id, e.g.
                             # seniruk/whisper-small-si (the default)
scipy                        # audio resampling for the transformers STT path

# Local TTS (default: TTS_ENGINE=piper)
piper-tts                   # provides the `piper` CLI; on Jetson ARM64 the
                             # prebuilt piper binary release may be more
                             # reliable than the pip wheel — see .env.example
